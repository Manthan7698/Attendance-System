module.exports = {
    darkMode: 'class', // this is critical
    theme: {
      extend: {
        colors: {
          bg: 'var(--bg)',
          text: 'var(--text)',
          primary: 'var(--primary)',
        },
      },
    },
    plugins: [],
  };
  